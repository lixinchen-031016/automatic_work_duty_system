# 自动值班排班系统 · 使用说明书

> 版本：v1.0　　适用程序：自动值班排班系统（DutyScheduler）

---

## 目录

- [第 1 章　程序功能简介](#第-1-章程序功能简介)
- [第 2 章　运行环境与安装步骤](#第-2-章运行环境与安装步骤)
- [第 3 章　界面总览](#第-3-章界面总览)
- [第 4 章　详细操作使用流程](#第-4-章详细操作使用流程)
- [第 5 章　参数与配置说明](#第-5-章参数与配置说明)
- [第 6 章　典型使用示例](#第-6-章典型使用示例)
- [第 7 章　常见问题与解决方法](#第-7-章常见问题与解决方法)
- [第 8 章　附录](#第-8-章附录)

---

## 第 1 章　程序功能简介

### 1.1 它能解决什么问题

传统排班靠"对着课表一格一格看"，人一多就必然出错：排到的时段有人在上课、个别人一学期值了十几次而有人一次没排、临时请假没人顶班。

本程序把这项工作自动化：

> **上传每位成员的个人课表 → 程序自动算出所有人"没课的空闲时段" → 按你设定的规则生成一张均衡、无冲突的值班表 → 一键导出 Excel / 图片发到群里。**

排班结果自动保存在本地数据库中，关掉程序不会丢；下周只需重新点一次"生成"，本周的班就排好了，之前几周的记录完全不受影响。

### 1.2 功能总览

| 模块 | 能力 |
| --- | --- |
| **课表管理** | 上传 `.xls` / `.xlsx` 个人课表，自动提取姓名、学号、班级、课程、教师、地点、周次、节次；重复上传同一人自动更新而非重复添加；支持搜索与按班级筛选；删除成员需二次确认 |
| **智能排班** | 5 条硬约束（绝不违反）+ 三级均衡 + 覆盖均衡 + 缺口链式修复；按期增量排班，历史周不受影响；结果自动落库 |
| **长期特殊安排** | 为某位成员添加"连续多周、每周固定星期/时段"的课表外占用（实习、训练、治疗等），排班与甘特图统一避让 |
| **请假登记** | 按成员 + 周次 + 星期登记临时请假及原因，排班自动避让，甘特图当天整行标红 |
| **手动微调** | 双击值班表格任一时段即可换人 / 新增 / 移除，候选实时标注"为什么不能用"；支持撤销 / 重做（最多 50 步） |
| **空闲甘特图** | 行 = 成员、列 = 星期 × 时段的全员忙闲矩阵；6 种颜色区分状态；高亮"全员空闲"时段；可导出带颜色的 Excel |
| **学期日历** | 登记法定假日与周末补课，支持从内置数据或在线数据一键导入国家节假日安排并预览确认 |
| **统计图表** | 各成员值班总次数、每周值班人次、各成员请假天数三张柱状图，可导出高清 PNG |
| **导出** | Excel（4 个 Sheet）/ CSV / PNG 图片 / 请假记录 Excel / 甘特图 Excel / 统计图 PNG |
| **数据安全** | 纯本地 SQLite 数据库，无联网、无服务器；存储位置可自定义；参数自动记忆，重启恢复 |

### 1.3 名词约定

| 名词 | 含义 |
| --- | --- |
| **值班时段块** | 程序把一天切成 5 个块：1-2节、3-4节、5-6节、7-8节、9-11节。一个"值班任务"= 某一周的某一天 + 某一个时段块 |
| **空闲** | 该时段块内的**每一节都没有课**。例如值"1-2节"要求第 1 节和第 2 节都无课 |
| **缺口（无人可用时段）** | 某个值班时段招不满"每时段人数"要求，属于客观排不出来，程序会列出原因 |
| **长期特殊安排** | 跨多周、每周固定重复的课表外占用（相当于临时给课表加一门"课"） |
| **请假** | 只在某一天的临时不可值班登记 |
| **按周增量** | 每次生成只重排"值班周范围"内的周，范围外的历史排班原样保留 |

---

## 第 2 章　运行环境与安装步骤

程序提供两种使用方式：**普通用户请用方式一（打包版）**，需要改代码或参与开发才用方式二。

### 2.1 环境要求

| 方式 | 要求 |
| --- | --- |
| 方式一 · 打包版 | Windows 10/11（64 位）　或　macOS（Apple Silicon / M 系列芯片） |
| 方式二 · 源码运行 | Python **3.10 及以上**（开发环境为 3.13），macOS / Windows / Linux 桌面环境均可 |

两种方式都**不需要联网**（仅"在线更新国家调休数据"这一个可选功能需要联网）。

---

### 2.2 方式一：使用打包版（推荐）

#### Windows

1. 下载 `DutyScheduler.exe`；
2. 放到一个**你自己可写**的文件夹（如 `D:\duty_system\`），**不要**放在 `C:\Program Files\` 下；
3. 双击运行，无需安装。

> 数据文件 `duty_system.db` 会优先创建在 exe 同目录（绿色软件，随 exe 一起移动）；若该目录不可写，自动改存到 `%LOCALAPPDATA%\DutySystem\`。

#### macOS

1. 下载 `DutyScheduler-macOS-arm64.dmg`，双击打开；
2. 把 `DutyScheduler.app` 拖入 `Applications` 文件夹；
3. **首次打开**时若提示"无法验证开发者"（程序未经 Apple 签名，属正常现象），任选一种方式解决：
   - 在"访达"中**右键点击**应用 → 选择「**打开**」→ 在弹窗中再次点「打开」；
   - 或在终端执行一次：`xattr -cr /Applications/DutyScheduler.app`

> 数据文件存放在 `~/Library/Application Support/DutySystem/`；若该目录不可写，自动回退到 `~/.dutysystem/`。

**打包版与源码运行的数据位置不同**，两者互不影响。左侧「数据存储」卡片在打包版中同样可用，可随时查看与更改。

---

### 2.3 方式二：从源码运行

#### macOS / Linux

```bash
# 1. 进入项目目录
cd automatic_work_duty_system

# 2. 创建并激活虚拟环境
python3 -m venv .venv
source .venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动
.venv/bin/python app.py
```

#### Windows（命令提示符 / PowerShell）

```bat
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
.venv\Scripts\python app.py
```

#### 依赖清单（`requirements.txt`）

| 包 | 用途 |
| --- | --- |
| `xlrd>=2.0.1` | 解析 `.xls` 格式课表 |
| `openpyxl>=3.1` | 解析 / 导出 `.xlsx` |
| `pandas>=2.0` | 表格构建 |
| `PySide6>=6.7` | 桌面界面 |
| `certifi>=2024.2` | 在线更新国家调休数据所需的 TLS 根证书 |

#### 启动异常的处理

若启动时报 **Qt 错误或 import 异常**（多见于某些 IDE / 终端环境的环境变量冲突），请在 macOS / Linux 下改用：

```bash
env -u PYTHONHOME -u PYTHONPATH .venv/bin/python app.py
```

Windows 正常执行 `.venv\Scripts\python app.py` 即可。

#### 卸载与重置

- 卸载源码版：删除项目目录即可；
- 卸载打包版：Windows 删除 exe 与同目录 `duty_system.db`；macOS 删除 `.app` 与 `~/Library/Application Support/DutySystem/`；
- **只重置界面参数、保留数据**：Windows 删除注册表项 `HKCU\Software\DutySystem\DutyScheduler`；macOS 删除对应的偏好设置文件。

---

### 2.4 第一次启动会看到什么

窗口打开后，右侧摘要区显示提示文字：

> **尚未生成排班表。设置左侧参数后点击「生成排班表」。**

此时左侧成员列表为空，各导出按钮均为灰色不可点——这是正常的，请继续阅读第 4 章。

---

## 第 3 章　界面总览

主窗口分为左右两部分：**左侧参数区固定不动，右侧六页签随操作切换**。

### 3.1 左侧面板

| 区域 | 控件 | 说明 |
| --- | --- | --- |
| 顶部 | **上传成员课表…** | 一次可选多个 `.xls` / `.xlsx` 文件 |
| 成员 | 搜索框 | 按姓名 / 学号 / 班级即时筛选 |
| | 班级下拉框 | 按班级筛选（默认「全部」） |
| | 成员列表 | **双击任一成员**可直接跳转到「成员课表」页并定位该人 |
| | 删除选中成员 | 红色按钮，会弹出确认框，**默认聚焦「取消」**以防误删 |
| 排班参数 | 值班周范围 | 决定本次重排哪些周 |
| | 值班星期 | 周一 ~ 周日复选，默认勾选周一至周五 |
| | 值班时段 | 5 个时段块复选，默认全选 |
| | 每时段人数 / 每周上限 / 每天上限 / 随机种子 | 详见第 5 章 |
| | 学期起始日 | 第一周周一的日期，用于把"周次"换算成"具体日期" |
| | 学期日历 | 显示已配置项数量，右侧「管理日历…」按钮打开配置窗口 |
| 主按钮 | **生成排班表** | 按当前参数生成（增量：范围外的周保留） |
| | 清空排班… | 可选择"仅清空所选周范围"或"清空全部排班" |
| 数据存储 | 数据库路径 | 悬停可查看完整路径；右侧「更改位置…」可迁移或切换数据库 |

### 3.2 右侧六个页签

| 页签 | 内容 | 可执行操作 |
| --- | --- | --- |
| **1. 值班排班表** | 周次 × 星期 的透视总表（含日期列） | **双击值班单元格手动微调**；导出 Excel / CSV / 图片 |
| **2. 值班明细** | 每行一个值班任务：周次、星期、日期、值班时段、值班人、人数 | 查看 |
| **3. 成员课表** | 单人逐周课表网格 + 长期特殊安排 + 请假登记 | 添加 / 编辑 / 删除特殊安排；添加 / 删除请假；导出请假记录 |
| **4. 值班统计** | 各成员值班次数统计 + 无人可用时段缺口清单（含成因分类） | 查看，按提示调整参数 |
| **5. 空闲甘特图** | 全员忙闲矩阵，可按周切换 | 导出甘特图 Excel |
| **6. 统计图表** | 三张柱状图：值班总次数 / 每周人次 / 请假天数 | 导出统计图 PNG |

### 3.3 菜单与快捷键

菜单栏「**编辑**」下提供手动微调的撤销 / 重做：

| 操作 | macOS | Windows |
| --- | --- | --- |
| 撤销微调 | `⌘ Z` | `Ctrl + Z` |
| 重做微调 | `⌘ ⇧ Z` | `Ctrl + Shift + Z` / `Ctrl + Y` |

最多可撤销 50 步。**切换成员或重新生成排班后，撤销历史会清空**，避免跨上下文误操作。

---

## 第 4 章　详细操作使用流程

整体流程共七步，第一次大约 10 分钟，熟练后每周只需 1 分钟。

```
① 准备课表 → ② 上传 → ③ 设参数 → ④ 补不可值班信息 → ⑤ 生成 → ⑥ 核对微调 → ⑦ 导出发布
```

---

### 第一步：准备课表文件

- 使用**教务系统导出的个人课表**（.xls 或 .xlsx），文件名通常是 `学生个人课表_学号.xls`。
- 每位成员一份文件，统一放在一个文件夹里便于一次多选。
- 无需手工整理格式：程序兼容常见写法——

| 情况 | 是否支持 |
| --- | --- |
| 合并单元格、单格多门课程、多门课之间无空行 | ✅ 支持 |
| 教师姓名断行、多人授课、职称括号换行 | ✅ 支持 |
| 混合周次 `1,3,5` / `1-10` | ✅ 支持 |
| 单双周 `1-16单周` / `1-16双周` / `1-16(单)` / `1-16奇周` | ✅ 自动展开为真实周次 |
| 星期表头写作 `星期一` / `周一` / `周1` / `礼拜一` / `星期7` | ✅ 均可识别 |
| 全角数字 `１８`、全角方括号 | ✅ 自动转半角 |
| 文件被改名（`.xls` 里其实是 xlsx 内容） | ✅ 按文件内容嗅探，不依赖扩展名 |

> ⚠️ 请勿手工改动教务系统导出的文件内容。程序刻意**不做** NFKC 归一化，以保证界面显示的课程名（如 `体育Ⅲ`、`（西四三楼）`）与教务系统完全一致。

---

### 第二步：上传成员课表

1. 点击左侧 **「上传成员课表…」**；
2. 在文件选择框中**按住 Ctrl / ⌘ 多选**所有成员的课表，点「打开」；
3. 观察结果：

| 现象 | 含义 |
| --- | --- |
| 成员列表出现姓名 + 课程数 | 解析成功 |
| 状态栏提示"新增 N 名成员" | 首次导入 |
| 状态栏提示"更新 N 份课表" | 该成员已存在（同学号 + 同姓名），课程被整体替换 |
| 弹出"部分文件导入失败" | 该文件不是教务系统个人课表，或文件损坏，请检查后重传 |
| 弹出"课表存在未定位的集中安排" | 备注行中的课程（如军训、认识实习）没有具体星期/节次，程序按整周避让——属正常处理 |

**常见做法**：期中教务处重新导出了某些同学的课表，直接再次上传这几份文件即可，会自动覆盖更新，不会产生重复成员。

---

### 第三步：设置排班参数

在左侧「排班参数」区确认以下项目（括号内为默认值）：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 值班周范围 | 1 至 18 | 本次要重排的周区间 |
| 值班星期 | 周一 ~ 周五 | 需要安排值班的星期 |
| 值班时段 | 5 个全选 | 需要安排值班的时段块 |
| 每时段人数 | 1 | 每个时段需要几个人同时在岗 |
| 每周上限 | 3 | 每人每周最多值几次班 |
| 每天上限 | 1 | 每人每天最多值几次班 |
| 随机种子 | 42 | 平手时决定分给谁；固定则结果可复现 |
| 学期起始日 | 今天所在周的周一 | **建议改为第一周周一的真实日期** |

> 💡 **强烈建议填写"学期起始日"**：填好后，值班表、明细表、导出的 Excel/CSV/图片都会自动带上每条值班的**具体日期**（支持跨月、跨年），发到群里无需再换算。

参数会**自动记忆**，下次打开程序时恢复上次的设置。

---

### 第四步：补充不可值班信息（可选但重要）

切到右侧「**成员课表**」页签，顶部的「选择成员」下拉框中先选中要操作的成员。

#### 4.4.1 长期特殊安排（跨多周的固定占用）

适用于：连续几周每周二下午固定实习、每周四晚上球队训练、固定时间的长期治疗等。

1. 在「长期特殊安排（修改课表）」卡片点击 **「添加安排…」**；
2. 在弹窗中填写：

| 字段 | 说明 |
| --- | --- |
| 连续周次 | 起止周，如"第 8 周 至 第 12 周" |
| 星期 | 下拉选择周一 ~ 周日 |
| 占用时段 | 复选一个或多个时段块，至少选一个 |
| 原因 | 如"固定实习"、"球队训练"，可留空 |

3. 点击 **「保存」**。

已添加的记录会显示在下方表格中，**双击某行即可编辑**，或选中后点「编辑选中…」/「删除选中」。

这类安排会被视为硬冲突——自动排班、甘特图和手动微调都会避让，甘特图中显示为紫色「其他安排」。

#### 4.4.2 请假登记（某一天的临时变化）

1. 在「请假登记（临时不可值班日）」卡片点击 **「添加请假…」**；
2. 填写**周次**、**星期**、**原因**（如"生病"、"比赛"，可留空），点「添加」；
3. 该成员当天任何时段都不再参与排班，甘特图中该天整行标红。

同一成员同一天重复登记会**自动覆盖**，不会产生重复记录。

需要报备或存档时，点 **「导出请假记录…」** 可导出全部成员的请假 Excel（含成员、学号、周次、星期、日期、原因）。

---

### 第五步：生成排班表

1. 点击左侧 **「生成排班表」**（这是最大的蓝色主按钮）；
2. 生成期间界面顶部显示"正在排班"，**界面不会卡死**，可继续操作；
3. 生成完成后自动跳到第 1 个页签，同时状态栏给出结果摘要，例如：

```
已重新排班 第5–5周：本次安排 25 人次（新增 25 / 移除 0），
范围外历史排班 425 人次保留，无人可用时段 2 个，缺口修复补上 3 个时段，
总次数极差 4，已避让 1 条请假记录。
```

摘要区（页面上方）也会显示一行总览：

```
排班完成：共 450 人次安排 | 参与成员 20 人 | 人均 22.5 次 |
总次数极差 4（越小越均衡）| 无人可用时段 2 个
```

**关键指标怎么看：**

| 指标 | 含义 | 期望 |
| --- | --- | --- |
| 本次安排人次 | 本次重排的周范围内共安排了多少人次 | — |
| 范围外历史排班保留 | 未受本次生成影响的旧记录数 | 越大说明历史越完整 |
| 总次数极差 | 最忙的人与最闲的人值班次数之差 | **越小越均衡**，理想值 ≤ 5 |
| 无人可用时段 | 排不出人的时段数量 | 越少越好，出现时请看下一节 |

---

### 第六步：核对与手动微调

#### 4.6.1 查看缺口成因

切到「**值班统计**」页签，下半部分列出所有"无人可用时段"，并给出**成因分类**：

| 主要原因 | 含义 | 该怎么做 |
| --- | --- | --- |
| 成员均有课、其他安排或请假 | 该时段**全员**都被课程/特殊安排/请假占用 | 只能靠**增加成员**或调整值班时段解决 |
| 受每天上限限制 | 有人空闲，但当天已经值过班了 | **提高"每天上限"** 或增加成员 |
| 受每周上限限制 | 有人空闲，但本周额度用完了 | **提高"每周上限"** 或增加成员 |
| 受每天/每周上限限制 | 两种上限同时卡住 | 同上 |
| 仍有空闲成员未被排入 | 理论上能排但没排上 | 重新生成一次，或双击单元格手动补排 |

#### 4.6.2 手动微调某一时段

1. 回到「**值班排班表**」页签；
2. **双击**要调整的单元格（表头的周次 / 星期 / 日期列除外）；
3. 弹出「手动微调」对话框：

   - 顶部显示：`第5周 周三 · 1-2节 08:30-10:05　当前值班：张三`
   - **调整对象**：选择要换掉的人；若该时段还没满员，会有「＋ 新增一人」选项
   - **新值班人**：候选列表，每项后面标注不可用原因——

   | 候选标注 | 能否选择 |
   | --- | --- |
   | 成员名（无标注） | ✅ 可换 |
   | 该时段有课 | ❌ 灰色不可选 |
   | 其他安排 | ❌ 灰色不可选 |
   | 请假 | ❌ 灰色不可选 |
   | 当天已值班 | ❌ 灰色不可选 |
   | 该时段已值班 | ❌ 灰色不可选 |
   | **本周已达上限** | ⚠️ **可选**——属"知情越限"，换入后该成员本周会超出上限，请自行权衡 |
   | （移除该值班人） | 移除当前值班人 |

4. 选中候选人后点 **「应用」**，界面与统计立即刷新。

5. 操作失误？用菜单「编辑 → 撤销」（`⌘Z` / `Ctrl+Z`）回退，或「重做」恢复。

---

### 第七步：导出与发布

在「值班排班表」页签下方有 **「导出 Excel」/「导出 CSV」/「导出图片」** 三个按钮。

**导出前会先弹窗询问要导出哪些周**：

- 全部周（第 1–18 周）
- 某一周（如"第 5 周"）——**每周发群选这个**

选择后按提示保存，文件名会自动带周次，如 `值班排班表_第5周.xlsx`。

**Excel 文件包含 4 个工作表：**

| Sheet | 内容 |
| --- | --- |
| 排班总表 | 周次 × 星期 的透视总表，列 = 各时段块 |
| 值班明细 | 每行一个值班任务（含日期列） |
| 值班统计 | 各成员总值班次数与值班周次 |
| 无人可用时段 | 缺口清单 + 主要原因 + 无课人数 |

其他导出入口：

| 位置 | 按钮 | 产出 |
| --- | --- | --- |
| 成员课表页 · 请假登记卡片 | 导出请假记录… | 请假记录 Excel |
| 空闲甘特图页 | 导出甘特图 Excel | 带填充色的忙闲矩阵（冻结首列） |
| 统计图表页 | 导出统计图 PNG | 三张柱状图纵向拼接的高清图片（2 倍分辨率） |

---

### 第八步：每周推进的标准动作（SOP）

学期初排一次整学期，之后每周固定做 4 件事即可，**历史周永远不会被改动**：

```
学期第 1 周（初始化）
  └ 周范围设为 1–18 → 「生成排班表」→ 一次性生成整学期

之后每周固定动作
  ① 有新情况先补录：
       · 连续多周的固定占用 → 成员课表页「添加安排…」
       · 只是某一天请假     → 成员课表页「添加请假…」
  ② 把「值班周范围」改成「本周 至 本周」（例如 5 至 5）
  ③ 点击「生成排班表」——只重排本周，其它周原样保留并计入均衡
  ④ 「导出 Excel / 图片」→ 选"第 5 周"→ 发到群里
```

---

### 第九步（进阶）：学期日历与节假日调休

如果学期中会放法定假日（如国庆）或遇周末补课，可在左侧「排班参数」区点 **「管理日历…」** 打开「学期日历覆盖」窗口。

**两种覆盖类型：**

| 类型 | 含义 |
| --- | --- |
| **放假 `off`** | 某个逻辑教学日放假，当天不排班、导出时整行跳过 |
| **补课 `class`** | 某个周末日期用来补某一天的课，需要指定它代表了"哪一周的星期几" |

**操作方式（三选一）：**

1. **手工添加**——点「添加覆盖项」，逐条选择日期与类型；
2. **一键添加调休**——点「一键添加调休」，输入放假日期，程序自动生成互补的补课项；
3. **从国家调休导入（推荐）**——点「从国家调休导入…」：
   - 选择**数据年份**（2004–2100）；
   - 点「**加载内置数据**」使用离线数据，或点「**在线更新**」从网络获取当年最新数据（需联网）；
   - 预览表格会列出"日期 / 类型 / 代表周 / 代表星期 / 备注"，**补课日对应的逻辑周与星期是自动推断结果，请逐行确认**，可删除不需要的行；
   - 点「**导入到日历**」合并到主列表。

保存前程序会执行**成对一致性校验**：

- 勾选「允许纯法定假日（off 无需补课配对）」（默认勾选）时，`off` 可以独立存在；
- 取消勾选后，每个 `off` 都必须有唯一的 `class` 项与之配对。

点「**校验**」可随时手动检查。校验不通过会明确提示原因，**不会写入非法配置**。

> 说明：排班引擎始终按"逻辑周次"运行，日历只负责在**展示与导出**时把逻辑周次映射为真实日期。原放假日标红，补课日按其代表的逻辑工作日课表判定忙闲。

---

### 第十步（进阶）：数据备份与迁移

| 需求 | 做法 |
| --- | --- |
| **备份全部数据** | 直接复制 `duty_system.db` 文件即可（成员、课表、排班、请假、日历、特殊安排全在里面） |
| **换电脑 / 恢复备份** | 把 `duty_system.db` 拷到新电脑，然后点击左侧「**更改位置…**」选中该文件所在目录——若目录中已有同名数据库，程序会询问是否**切换为使用该文件**（不会覆盖任何数据） |
| **迁移到其它目录** | 点「更改位置…」选择目标文件夹，程序会把现有数据**安全复制**过去（原文件保留），并自动切换 |
| **只改界面参数** | 见第 2.3 节"卸载与重置" |

> ⚠️ 请勿在程序运行时用文件管理器直接复制数据库文件。程序使用 SQLite 的 WAL 模式，最新数据可能还在 `-wal` 附属文件中。程序内部的「更改位置」功能走的是 SQLite 在线备份接口，因此是安全的。

---

## 第 5 章　参数与配置说明

### 5.1 排班参数详解

| 参数 | 控件 | 取值范围 | 默认值 | 作用与调整建议 |
| --- | --- | --- | --- | --- |
| **值班周范围** | 两个数字框（起 / 至） | 1 ~ 25 | 1 至 18 | 决定**本次重排哪些周**。设成整个学期 = 全量重排；设成"本周至本周" = 只重排本周。范围外的历史排班原样保留，并计入均衡基数 |
| **值班星期** | 7 个复选框 | 周一 ~ 周日 | 周一~周五 | 勾上的星期才会安排值班。若只有工作日需要值班，保持默认即可 |
| **值班时段** | 5 个复选框 | 1-2节 / 3-4节 / 5-6节 / 7-8节 / 9-11节 | 全选 | 需要安排值班的时段块。**至少要勾一个**，否则无法生成 |
| **每时段人数** | 数字框 | 1 ~ 5 | 1 | 每个时段需要几人同时在岗。加大后总需求翻倍，请确认空闲成员是否够用 |
| **每周上限** | 数字框 | 1 ~ 10 | 3 | 每人每周最多值几次班。**软约束**——手动微调时可"知情越限" |
| **每天上限** | 数字框 | 1 ~ 5 | 1 | 每人每天最多值几次班。**硬约束** |
| **随机种子** | 数字框 | 0 ~ 9999 | 42 | 多人条件完全相同时决定分给谁。**固定种子 = 同参数生成完全相同的排班**（可复现）；换一个种子 = 换一版同样均衡的方案 |
| **学期起始日** | 日期选择器 | — | 今天所在周的周一 | 第一周周一的真实日期。填写后所有视图与导出均显示具体日期 |
| **甘特图查看周次** | 数字框（甘特图页） | 1 ~ 25 | 1 | 甘特图的显示周次（课程随周次变化） |

### 5.2 排班规则说明

#### 硬约束（算法保证，手动微调也不可越过）

1. **无课冲突**——值班时段块内的每一节都不能有课；
2. **特殊安排冲突**——有长期特殊安排占用的时段不可安排；
3. **请假日不可值班**——请假当天任何时段都不参与排班；
4. **每天一次**——每人每天最多值 1 次班（由"每天上限"控制）；
5. **每周上限**——每人每周不超过设定次数（自动排班中严格执行）。

#### 软目标（尽量做到均衡）

- **三级贪心均衡**：优先安排「累计总次数最少 → 本周次数最少 → 当天次数最少」的成员；
- **动态覆盖均衡**：优先安排当前覆盖最少的「星期 × 时段」组合，保证每天都有值班，避免出现"周五永远没人"；
- **同覆盖度时按时段稀缺度排序**：可选人数少的时段优先占位；
- **缺口链式修复**：贪心结束后对剩余缺口做深度 2 的挪动修复（把已排的人挪到缺人时段，再回填其原时段）。

> 程序在 20~30 人的典型场景下，值班覆盖率可达理论最优值的 **99% 以上**；同时"总次数极差"远低于纯数学最优解（后者虽然覆盖率满分，却会出现有人值 54 次、有人值 18 次的不公平结果）。这是刻意取舍的结果。

### 5.3 六个页签的字段含义

**值班排班表 / 值班明细**（含日期的前提是设置了"学期起始日"）

| 列 | 含义 |
| --- | --- |
| 周次 | 如"第 5 周" |
| 星期 | 如"周三" |
| 日期 | 该值班的真实日期（跨月/跨年自动正确） |
| 值班时段 | 如"1-2节 08:30-10:05" |
| 值班人 | 多人用顿号连接 |
| 人数 | 该时段实际在岗人数 |

**值班统计（上半部分）**：成员 / 总值班次数 / 值班周次（按总次数从多到少排序）。
**值班统计（下半部分）**：周次 / 星期 / 时段 / 主要原因 / 无课人数。

**成员课表页**：顶部下拉框格式为「姓名（班级）」；课表网格列为 星期 / 课程 / 教师 / 周次 / 节次 / 地点；下方"长期特殊安排"表列为 周次 / 星期 / 时段 / 原因，请假表列为 周次 / 星期 / 原因。

**空闲甘特图**：行 = 成员，列 = 星期 × 时段；底部汇总行显示各时段的空闲人数。

| 颜色 | 含义 |
| --- | --- |
| 🟩 浅绿 | 空闲 |
| ⬜ 浅灰 | 有课 |
| 🟦 浅蓝 | 已排值班 |
| 🟧 橙色 | **全员空闲**（适合安排需要全员参加的任务） |
| 🟥 浅红 | 请假 |
| 🟪 浅紫 | 其他安排（长期特殊安排） |

### 5.4 数据存储与持久化配置

**数据库文件**：单一 SQLite 文件 `duty_system.db`，默认位置见下表。

| 运行方式 | 数据库默认位置 |
| --- | --- |
| 源码运行 | 程序目录下的 `duty_system.db` |
| Windows 打包版 | exe 同目录；不可写时回退 `%LOCALAPPDATA%\DutySystem\` |
| macOS 打包版 | `~/Library/Application Support/DutySystem/`；不可写时回退 `~/.dutysystem/` |

**数据库表结构**：

| 表 | 内容 |
| --- | --- |
| `members` | 成员（学号 + 姓名唯一，重复上传即更新） |
| `courses` | 成员课程记录 |
| `special_arrangements` | 长期特殊安排（修改课表） |
| `duty_assignments` | 值班安排 |
| `leaves` | 请假记录 |

删除成员时，其课程、特殊安排、值班、请假记录会**级联删除**。

**界面参数**存于操作系统偏好设置（QSettings），组织名 `DutySystem`，应用名 `DutyScheduler`：

| 存储项 | 说明 |
| --- | --- |
| `database/path` | 自定义的数据库路径；若该路径已不存在（如移动硬盘拔出），自动回退到默认位置 |
| 周范围 / 星期 / 时段 / 上限 / 种子 / 起始日 等 | 各参数控件值 |
| `schedule/last_config/<数据库路径>` | 上次生成排班时使用的参数（按数据库文件分别记录，切换数据库互不干扰） |

---

## 第 6 章　典型使用示例

### 示例一：实验室值班表（最常见场景）

**场景**：20 名同学，工作日 5 个时段均需安排，每个时段 2 人在岗，学期 18 周。

**操作**：

| 步骤 | 操作 | 参数值 |
| --- | --- | --- |
| 1 | 上传 20 份课表 | — |
| 2 | 设置参数 | 周范围 1–18；星期 周一~周五；时段 全选 |
| | | **每时段人数 2**；每周上限 3；每天上限 1；种子 42 |
| | | 学期起始日 = 第一周周一 |
| 3 | 点「生成排班表」 | — |
| 4 | 查看摘要 | 总需求 18 周 × 5 天 × 5 时段 × 2 人 = **900 人次** |
| 5 | 导出 Excel → 全部周 | 得到整学期总表 |

**预期结果**：约 875~900 人次成功安排（覆盖率 > 97%），总次数极差约 4，剩余少量缺口分散在个别时段。若有缺口，按第 4.6.1 节的成因分类处理。

---

### 示例二：按周推进（推荐工作方式）

**场景**：班主任希望每周一发本周值班表，且不想每周都重排整学期。

**操作**：

| 时间 | 操作 |
| --- | --- |
| 开学第 1 周 | 周范围 **1–18** → 生成 → 导出全部周，贴在班级公告栏 |
| 每周一 | ① 补录新情况（特殊安排 / 请假）<br>② 周范围改为 **本周 至 本周**（如 `5 至 5`）<br>③ 生成排班表<br>④ 导出 → 选"第 5 周" → 发群 |

**预期结果**：每周只重排当周，之前几周的记录完整保留，长期看总次数依然均衡（历史次数会计入均衡基数）。本周有人请假时，重排会自动把班转移给本周空闲最多的人。

---

### 示例三：某成员连续三周固定实习

**场景**：李四第 8–10 周每周三下午 5-6 节、7-8 节都在校外实习。

**操作**：

1. 「成员课表」页 → 选择成员「李四」；
2. 「长期特殊安排」卡片 → 「添加安排…」；
3. 填写：连续周次 **8 至 10**；星期 **周三**；占用时段勾选 **5-6节** 与 **7-8节**；原因"校外实习"；
4. 保存 → 右侧摘要区出现"⚠ 排班参数或成员已变化，下方结果可能过期"提示 → 点「生成排班表」重新生成。

**预期结果**：这 3 周的周三下午不再排李四的班；甘特图中对应格子显示紫色"其他安排"；若该时段人手不足，缺口清单会给出原因。

---

### 示例四：临时请假后重新生成

**场景**：王五第 6 周周四突然生病，需要请假。

**操作**：

1. 「成员课表」页 → 选择成员「王五」；
2. 「请假登记」卡片 → 「添加请假…」；
3. 周次 **6**，星期 **周四**，原因"生病" → 添加；
4. 把周范围改为 **6 至 6**，点「生成排班表」；
5. 检查摘要中"无人可用时段"是否为 0；若某时段无人可替，双击该单元格手动微调或选择"维持原安排"。

**预期结果**：第 6 周周四王五的班被重新分配；甘特图中该天整行标红；其它周不受影响。

---

### 示例五：国庆节放假 + 周末补课

**场景**：学期第一周周一为 2026-09-01，10 月 1–7 日放假，9 月 27 日（周日）补 10 月 2 日（周五）的课。

**操作**：

1. 左侧「管理日历…」→ 打开「学期日历覆盖」窗口；
2. 点「**从国家调休导入…**」→ 年份选 **2026** → 点「**加载内置数据**」；
3. 在预览表中逐行核对"代表周 / 代表星期"是否正确（补课日会自动推断）；
4. 点「**导入到日历**」；
5. 点「**校验**」确认无冲突 → 点「**保存**」。

**预期结果**：左侧"学期日历"显示如"8 项（放假 6 / 补课 2）"；导出的值班表中，10 月 1–7 日不再出现排班，补课日显示真实日期与保留的星期标签。

---

### 示例六：某时段长期排不出人

**场景**：周三 5-6 节始终有 3 个缺口。

**排查步骤**：

1. 切到「**值班统计**」页签，查看"无人可用时段"清单中周三 5-6 节的「主要原因」；

   | 显示的原因 | 处理方式 |
   | --- | --- |
   | 成员均有课、其他安排或请假 | 该时段全员满课——**改动值班时段**（如取消勾选 5-6节）或吸纳更多成员 |
   | 受每周上限限制 | 把「每周上限」从 3 调到 4，重新生成 |
   | 受每天上限限制 | 把「每天上限」从 1 调到 2，重新生成 |
   | 仍有空闲成员未被排入 | 重新生成一次；若仍有缺口，双击单元格手动补排 |

2. 也可切到「空闲甘特图」页，把"查看周次"设为出现缺口的那一周，直接看到该时段每人的忙闲情况。

---

## 第 7 章　常见问题与解决方法

### 7.1 安装与启动

**Q1：启动时报 Qt 错误或 import 异常？**
多为环境变量冲突。macOS / Linux 下改用：
`env -u PYTHONHOME -u PYTHONPATH .venv/bin/python app.py`

**Q2：macOS 提示"无法验证开发者"，打不开应用？**
程序未经 Apple 签名，属正常现象。右键点击应用 →「打开」→ 再点「打开」；或先执行 `xattr -cr /Applications/DutyScheduler.app`。

**Q3：Windows 上提示数据库无法创建 / 数据丢失？**
程序装在 `C:\Program Files` 等只读目录下会导致无法写入。请把 exe 移到用户目录（如 `D:\duty_system\`）；或保持现状——程序会自动把数据库存到 `%LOCALAPPDATA%\DutySystem\`。

**Q4：能在 Windows 上运行吗？**
能，源码无需任何修改。注意 Python 版本需 **3.10 及以上**（代码使用了 `X | None` 联合类型语法）。

**Q5：双击 exe 一闪而过 / 没有窗口？**
打包版是无控制台窗口的单文件程序；请确认下载完整，并检查杀毒软件是否拦截。

---

### 7.2 课表上传与解析

**Q6：提示"未解析到课程信息，请确认是教务系统导出的个人课表"？**
说明该文件不是标准的个人课表，或文件损坏。请回到教务系统重新导出。

**Q7：文件明明是 .xls，为什么能正常读取？/ 我改了扩展名会不会出错？**
不会。程序**按文件内容嗅探格式**，不依赖扩展名。教务系统导出的文件被改名（`.xls` 里其实是 xlsx）也能正确解析。

**Q8：上传同一份课表会不会出现两个成员？**
不会。程序按「学号 + 姓名」识别，判定为同一人时**整体替换其课程**，并提示"更新 N 份课表"。

**Q9：提示"课表存在未定位的集中安排"，会影响排班吗？**
不会出错。这是课表末尾备注行中的集中实践课程（如军训、认识实习）没有具体星期/节次。程序把它们按**整周避让**处理，即那些周不会给该成员排班——这是安全的保守处理。

**Q10：界面上的课程名显示为 `体育Ⅲ`，是不是乱码？**
不是。程序**刻意不做**全角→半角 / 罗马数字的强制归一化，以保证显示与教务系统完全一致（否则会变成 `体育III`）。

**Q11：担心课表里的学号姓名泄露？**
所有数据仅保存在你本机的数据库文件中，程序**不上传任何数据**。若要分享课表文件，请自行脱敏；项目源码中提交的样例课表已做等长字节脱敏处理。

---

### 7.3 排班结果

**Q12：为什么周五（或某天）排不出值班？**
程序已采用"动态覆盖均衡"，优先安排覆盖最少的星期，正常情况下不会出现某天被系统性忽略。若仍缺口，请查看「值班统计」页的"无人可用时段"清单——那表示该时段全员有课或请假，属客观不可排。

**Q13：重新生成会覆盖以前的排班吗？**
**不会。** 只重排"值班周范围"内的周；范围外的历史排班原样保留并计入均衡基数。想整学期重排，把周范围设为整个学期再生成。

**Q14：关掉程序后排班结果还在吗？**
在。结果实时写入数据库，重新打开程序会自动恢复，状态栏提示"已恢复上次的排班结果"。

**Q15：改了参数或成员后，旧结果还能看吗？**
能看，但会出现"⚠ 排班参数或成员已变化，结果可能过期"的提示，建议重新生成。

**Q16：怎么让排班结果可复现？**
保持"随机种子"不变，且成员、课表、参数一致，则每次生成的结果完全相同。换个种子可以得到另一版同样均衡的方案。

**Q17：某人值班次数明显偏多/偏少？**
先看「值班统计」页的"总值班次数"列。若极差较大，请检查：① 是否有成员长期请假或被特殊安排占用大量时段；② 是否"值班周范围"设得太窄（历史次数计入基数需要范围外存在记录）。适当调大周范围重新生成可改善。

**Q18：想让某些人少排一点？**
当前版本不支持按人设置权重。可行的替代做法：用"长期特殊安排"为其添加固定的不可值班时段，间接降低其可排次数。

---

### 7.4 手动微调

**Q19：双击单元格没反应？**
请双击「**值班排班表**」页中**表格内容区的值班单元格**；表头的周次 / 星期 / 日期列不可双击。

**Q20：候选列表里的人都是灰色的，选不了？**
灰色表示该成员在当前时段属于**硬冲突**（有课 / 其他安排 / 请假 / 当天已值班 / 该时段已值班），无法换入。请查看前面标注的原因。

**Q21：为什么"本周已达上限"的人还能选？**
这是刻意保留的**软约束逃生口**。当你明知会超出每周上限但仍需要该成员顶班时，可以换入；系统会给出提示，由你自行决定。

**Q22：换错了人怎么办？**
菜单「编辑 → 撤销」（`⌘Z` / `Ctrl+Z`），最多可回退 50 步。注意切换成员或重新生成排班后撤销历史会清空。

---

### 7.5 导出

**Q23：导出的 Excel 里只有周次，没有具体日期？**
请在左侧填写「**学期起始日**」（第一周周一的日期），之后明细表、透视表、Excel、CSV、PNG 都会自动带上日期列。

**Q24：导出的 CSV 在 Excel 里打开是乱码？**
不会。程序使用 UTF-8 BOM 编码，Windows 版 Excel 双击可直接正确打开。

**Q25：导出文件里能只有某一周吗？**
可以。导出前会弹窗选择"全部周"或某一周，文件名自动带周次（如 `值班排班表_第5周.xlsx`）。

**Q26：Excel 导出很慢或界面卡住？**
导出在后台线程执行，界面不会卡死。成员数量大（数百人）时耗时较长属正常，完成后状态栏会提示文件保存位置。

**Q27：导出的图片适合直接发群吗？**
适合。「导出图片」生成 2 倍分辨率 PNG，标题与周次日期范围会自动渲染在图上，方便发群通知或打印张贴。

---

### 7.6 数据与配置

**Q28：换电脑后数据怎么带过去？**
复制 `duty_system.db` 到新电脑，再用左侧「更改位置…」选择该文件所在目录，程序会询问是否切换到该数据库。

**Q29：能恢复之前的备份吗？**
能。把备份的 `duty_system.db` 放到某个目录，用「更改位置…」选中该目录，确认"切换为使用该数据库文件"即可。**原数据库不会被覆盖**。

**Q30：为什么不能直接复制数据库文件做备份？**
运行时可以复制（数据都在主库里），但程序内部用的是 WAL 模式 + SQLite 在线备份接口以确保完整。**建议关掉程序后再复制**，或用「更改位置…」功能做迁移，最为稳妥。

**Q31：删掉了某个成员，能恢复吗？**
不能。删除会级联清除该成员的课程、特殊安排、值班和请假记录。删除前请确认，或先备份数据库文件。

**Q32：清空排班有哪些选择？**
点左侧「清空排班…」，可选：
- **清空所选周范围**——只清掉当前"值班周范围"内的安排；
- **清空全部排班**——删除所有值班记录，**不可恢复**，操作前会有二次确认。

---

## 第 8 章　附录

### 附录 A　时段块对照表

| 时段块 | 覆盖节次 | 时间 |
| --- | --- | --- |
| 1-2节 | 第 1、2 节 | 08:30 – 10:05 |
| 3-4节 | 第 3、4 节 | 10:20 – 11:55 |
| 5-6节 | 第 5、6 节 | 14:00 – 15:35 |
| 7-8节 | 第 7、8 节 | 15:50 – 17:25 |
| 9-11节 | 第 9、10、11 节 | 18:50 – 21:15 |

> 判定规则：要值班"1-2节"，必须第 1 节和第 2 节**都**没有课。

### 附录 B　导出文件一览

| 触发位置 | 格式 | 默认文件名 | 内容 |
| --- | --- | --- | --- |
| 值班排班表页 | Excel | `值班排班表_第5周.xlsx` | 4 个 Sheet：排班总表 / 值班明细 / 值班统计 / 无人可用时段 |
| 值班排班表页 | CSV | `值班排班表_第5周.csv` | 值班明细，UTF-8 BOM |
| 值班排班表页 | PNG | `值班排班表_第5周.png` | 总表渲染图，2 倍分辨率 |
| 成员课表页 · 请假登记 | Excel | — | Sheet「请假记录」：成员、学号、周次、星期、日期、原因 |
| 空闲甘特图页 | Excel | `空闲甘特图_第5周.xlsx` | 带填充色的忙闲矩阵，冻结首列 |
| 统计图表页 | PNG | `值班请假统计图.png` | 三张柱状图纵向拼接，2 倍分辨率 |

### 附录 C　课表常见写法兼容性速查

| 写法 / 情况 | 示例 | 支持 |
| --- | --- | --- |
| 混合周次 | `1,3,5` / `1-10` | ✅ |
| 单双周 | `1-16单周` / `1-16双周` / `1-16(单)` / `1-16奇周` | ✅ 展开为真实周次 |
| 星期表头 | `星期一` / `周一` / `周1` / `礼拜一` / `星期7` | ✅ |
| 全角字符 | `１８`、`【】` | ✅ 转半角 |
| 合并单元格 | — | ✅ |
| 单格多门课程、课程间无空行 | — | ✅ |
| 教师名断行 / 多人授课 / 职称括号换行 | — | ✅ |
| 无地点课程 / 同课换教室 | — | ✅ |
| 集中实践（军训、认识实习等无具体节次） | — | ⚠️ 按整周避让 |
| 文件扩展名与实际格式不符 | `.xls` 实为 xlsx | ✅ 按内容嗅探 |

### 附录 D　最小工作流速查卡

```
① 上传成员课表…（可多选）
② 周范围 = 1 至 18 ｜ 勾选值班星期 / 时段 ｜ 填学期起始日
③ （可选）成员课表页 → 添加安排… / 添加请假…
④ 生成排班表
⑤ 看摘要：无人可用时段 是否偏多 → 值班统计页看成因
⑥ （可选）双击单元格微调 → ⌘Z / Ctrl+Z 撤销
⑦ 导出 Excel / 图片 → 选周次 → 发群

每周重复：补录情况 → 周范围改为"本周至本周" → 生成 → 导出本周
```

---

*本说明书基于程序当前版本编写，所有操作路径、参数默认值与界面文字均与程序实际实现一致。*
